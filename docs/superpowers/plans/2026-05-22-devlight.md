# DevLight Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 ESP32 三色灯做成 Claude Code + Codex 的开发状态指示灯(绿=完成/空闲、黄闪=开发中、红闪=等确认、三灯慢闪=离线),通过 WiFi+HTTP 接收状态。

**Architecture:** ESP32 联 WiFi、注册 mDNS `devlight.local`、起 WebServer 接收 `GET /set?agent=&state=`;本地持有 claude/codex 两个子状态,按 红>黄>绿 聚合并用 `millis()` 非阻塞驱动 LED。Mac 侧:Claude Code hooks 与 Codex notify 脚本各发一行非阻塞 curl。无 Mac 常驻进程。

**Tech Stack:** ESP32 Arduino core 3.3.8-cn(自带 `WiFi`/`WebServer`/`ESPmDNS`/`DNSServer`/`Preferences`,无额外库);bash + curl;arduino-cli。

**硬件/烧录固定参数:** 接线 25=G/26=Y/27=R/-=GND(共阴,高电平点亮);FQBN `esp32:esp32:esp32`;端口 `/dev/cu.wchusbserial10`。

**编译/烧录命令(全程绕代理,core 已装):**
```bash
unset http_proxy https_proxy all_proxy HTTP_PROXY HTTPS_PROXY ALL_PROXY
/opt/homebrew/bin/arduino-cli compile --fqbn esp32:esp32:esp32 /Users/bytedance/esp32-devlight
/opt/homebrew/bin/arduino-cli upload -p /dev/cu.wchusbserial10 --fqbn esp32:esp32:esp32 /Users/bytedance/esp32-devlight
```
**看串口日志:** `/opt/homebrew/bin/arduino-cli monitor -p /dev/cu.wchusbserial10 -c baudrate=115200`

---

## File Structure

- `esp32-devlight/esp32-devlight.ino` — 固件主文件(联网、HTTP、状态机、LED 驱动、配网)。单文件,~250 行,职责集中,便于在一个上下文里读完。
- `esp32-devlight/secrets.h` — 本地可选 WiFi 默认凭据(gitignored)。
- `esp32-devlight/secrets.h.example` — 模板,进版本库。
- `esp32-devlight/.gitignore` — 忽略 `secrets.h` 与构建产物。
- `esp32-devlight/devlight-codex-notify.sh` — Codex notify 程序,解析事件 JSON → curl。
- `~/.claude/settings.json` — 追加 hooks(保留现有 SessionStart/SessionEnd)。
- `~/.codex/config.toml` — 设置 `notify`。

> 验证以"烧录+观察灯/串口/`GET /`"为主(硬件在环);纯逻辑(聚合、脚本解析)能离线验证的就离线验证。

---

## Task 1: 项目脚手架 + secrets 机制

**Files:**
- Create: `esp32-devlight/secrets.h.example`
- Create: `esp32-devlight/secrets.h`
- Create: `esp32-devlight/.gitignore`

- [ ] **Step 1: 写 secrets 模板**

`esp32-devlight/secrets.h.example`:
```cpp
#pragma once
// 可选:预置 WiFi 默认凭据。留空则首次开机进 captive portal 配网。
#define WIFI_SSID ""
#define WIFI_PASS ""
```

- [ ] **Step 2: 写本地 secrets.h(执行时由用户填真实凭据)**

`esp32-devlight/secrets.h`(先与模板相同;执行阶段询问用户 SSID/密码后填入,绝不回显、不入库):
```cpp
#pragma once
#define WIFI_SSID ""
#define WIFI_PASS ""
```

- [ ] **Step 3: 写 .gitignore**

`esp32-devlight/.gitignore`:
```
secrets.h
build/
*.bin
```

- [ ] **Step 4: 提交(若已 git init;否则跳过)**

```bash
cd /Users/bytedance/esp32-devlight && git add secrets.h.example .gitignore 2>/dev/null || true
```

---

## Task 2: 固件骨架 — 联 WiFi + mDNS + `GET /` 调试页

目标里程碑:烧录后串口打印 IP,Mac 上 `curl http://devlight.local/` 能拿到当前状态文本。

**Files:**
- Create: `esp32-devlight/esp32-devlight.ino`

- [ ] **Step 1: 写固件骨架(联网 + mDNS + `/`)**

`esp32-devlight/esp32-devlight.ino`:
```cpp
#include <WiFi.h>
#include <WebServer.h>
#include <ESPmDNS.h>
#include <DNSServer.h>
#include <Preferences.h>

#if __has_include("secrets.h")
  #include "secrets.h"
#endif
#ifndef WIFI_SSID
  #define WIFI_SSID ""
#endif
#ifndef WIFI_PASS
  #define WIFI_PASS ""
#endif

const int PIN_G = 25, PIN_Y = 26, PIN_R = 27;

WebServer server(80);
Preferences prefs;

// 主机名 = devlight-<设备名>;设备名空时回退芯片 MAC 后4位hex(多盏灯不撞名)
String deviceHostname() {
  String name = prefs.getString("name", "");
  if (name.length() == 0) {
    uint64_t mac = ESP.getEfuseMac();
    char suf[5]; snprintf(suf, sizeof(suf), "%04x", (uint16_t)(mac & 0xFFFF));
    name = String(suf);
  }
  return String("devlight-") + name;
}

enum SubState { ST_IDLE, ST_WORKING, ST_CONFIRM };
SubState claudeState = ST_IDLE;
SubState codexState  = ST_IDLE;

const char* subName(SubState s){ return s==ST_CONFIRM?"confirm":s==ST_WORKING?"working":"idle"; }

void handleRoot() {
  String body = String("claude=") + subName(claudeState) +
                "\ncodex=" + subName(codexState) + "\n";
  server.send(200, "text/plain", body);
}

void connectWifi() {
  String ssid = prefs.getString("ssid", WIFI_SSID);
  String pass = prefs.getString("pass", WIFI_PASS);
  Serial.printf("Connecting to '%s'...\n", ssid.c_str());
  String host = deviceHostname();
  WiFi.mode(WIFI_STA);
  WiFi.setHostname(host.c_str());
  WiFi.begin(ssid.c_str(), pass.c_str());
  unsigned long t0 = millis();
  while (WiFi.status() != WL_CONNECTED && millis() - t0 < 20000) { delay(250); Serial.print("."); }
  Serial.println();
  if (WiFi.status() == WL_CONNECTED) {
    Serial.print("IP: "); Serial.println(WiFi.localIP());
    if (MDNS.begin(host.c_str())) { MDNS.addService("http","tcp",80); Serial.printf("mDNS: %s.local\n", host.c_str()); }
  } else {
    Serial.println("WiFi connect FAILED");
  }
}

void setup() {
  Serial.begin(115200);
  pinMode(PIN_G, OUTPUT); pinMode(PIN_Y, OUTPUT); pinMode(PIN_R, OUTPUT);
  prefs.begin("wifi", false);
  connectWifi();
  server.on("/", handleRoot);
  server.begin();
}

void loop() {
  server.handleClient();
}
```

- [ ] **Step 2: 临时填测试 WiFi(执行时用用户给的凭据或手机热点),编译**

```bash
unset http_proxy https_proxy all_proxy HTTP_PROXY HTTPS_PROXY ALL_PROXY
/opt/homebrew/bin/arduino-cli compile --fqbn esp32:esp32:esp32 /Users/bytedance/esp32-devlight
```
Expected: 编译通过,打印 Sketch uses ... bytes。

- [ ] **Step 3: 烧录并看串口**

```bash
/opt/homebrew/bin/arduino-cli upload -p /dev/cu.wchusbserial10 --fqbn esp32:esp32:esp32 /Users/bytedance/esp32-devlight
/opt/homebrew/bin/arduino-cli monitor -p /dev/cu.wchusbserial10 -c baudrate=115200
```
Expected: 串口打印 `IP: x.x.x.x` 和 `mDNS: devlight.local`。

- [ ] **Step 4: 从 Mac 验证可达**

用串口打印的主机名(默认 `devlight-<芯片4位>.local`):
```bash
unset http_proxy https_proxy all_proxy HTTP_PROXY HTTPS_PROXY ALL_PROXY
HOST=devlight-XXXX.local   # 替换成串口打印的实际名字
curl -s --max-time 2 "http://$HOST/" ; echo
```
Expected: 输出
```
claude=idle
codex=idle
```
若 `.local` 解析失败,改用串口里的 IP 验证(并记入 spec §6 第 3 点)。

- [ ] **Step 5: 提交**

```bash
cd /Users/bytedance/esp32-devlight && git add esp32-devlight.ino 2>/dev/null || true
```

---

## Task 3: `/set` 端点 + 状态聚合(红>黄>绿)

**Files:**
- Modify: `esp32-devlight/esp32-devlight.ino`(加 `/set`、聚合函数、`/` 增加聚合显示)

- [ ] **Step 1: 加聚合函数与解析助手(放在 `handleRoot` 之前)**

```cpp
enum Agg { AGG_GREEN, AGG_YELLOW, AGG_RED };
Agg aggregate() {
  if (claudeState==ST_CONFIRM || codexState==ST_CONFIRM) return AGG_RED;
  if (claudeState==ST_WORKING || codexState==ST_WORKING) return AGG_YELLOW;
  return AGG_GREEN;
}
const char* aggName(Agg a){ return a==AGG_RED?"red":a==AGG_YELLOW?"yellow":"green"; }

bool parseState(const String& v, SubState& out) {
  if (v=="idle"){out=ST_IDLE;return true;}
  if (v=="working"){out=ST_WORKING;return true;}
  if (v=="confirm"){out=ST_CONFIRM;return true;}
  return false;
}
```

- [ ] **Step 2: 加 `/set` handler**

```cpp
void handleSet() {
  String agent = server.arg("agent");
  String state = server.arg("state");
  SubState parsed;
  if ((agent!="claude" && agent!="codex") || !parseState(state, parsed)) {
    server.send(400, "text/plain", "bad agent/state\n");
    return;
  }
  if (agent=="claude") claudeState = parsed; else codexState = parsed;
  Serial.printf("set %s=%s -> agg=%s\n", agent.c_str(), state.c_str(), aggName(aggregate()));
  server.send(200, "text/plain", "ok\n");
}
```

- [ ] **Step 3: `/` 增加聚合行;注册 `/set`**

`handleRoot` 的 body 改为:
```cpp
  String body = String("claude=") + subName(claudeState) +
                "\ncodex=" + subName(codexState) +
                "\nagg=" + aggName(aggregate()) + "\n";
```
`setup()` 的 server 注册处加:
```cpp
  server.on("/set", handleSet);
```

- [ ] **Step 4: 编译、烧录(同 Task 2 命令)**

- [ ] **Step 5: 验证聚合优先级**

```bash
unset http_proxy https_proxy all_proxy HTTP_PROXY HTTPS_PROXY ALL_PROXY
B=http://devlight.local
curl -s "$B/set?agent=claude&state=working"; curl -s "$B/set?agent=codex&state=confirm"
curl -s "$B/"; echo "--- 期望 agg=red(codex confirm 压过 claude working) ---"
curl -s "$B/set?agent=codex&state=idle"; curl -s "$B/"; echo "--- 期望 agg=yellow ---"
curl -s "$B/set?agent=claude&state=idle"; curl -s "$B/"; echo "--- 期望 agg=green ---"
curl -s "$B/set?agent=foo&state=bar" -w " [HTTP %{http_code}]\n"; echo "--- 期望 400 ---"
```
Expected: 依次 `agg=red` → `agg=yellow` → `agg=green`,非法参数 `HTTP 400`。

- [ ] **Step 6: 提交**

---

## Task 4: LED 非阻塞驱动(绿常亮 / 黄闪 / 红闪)

**Files:**
- Modify: `esp32-devlight/esp32-devlight.ino`(加 LED 渲染,改 `loop`)

- [ ] **Step 1: 加 LED 渲染函数(基于 millis,2Hz 闪烁)**

```cpp
void renderLeds() {
  Agg a = aggregate();
  // 2Hz: 250ms 亮 / 250ms 灭
  bool blinkOn = (millis() / 250) % 2 == 0;
  digitalWrite(PIN_G, a==AGG_GREEN ? HIGH : LOW);
  digitalWrite(PIN_Y, (a==AGG_YELLOW && blinkOn) ? HIGH : LOW);
  digitalWrite(PIN_R, (a==AGG_RED   && blinkOn) ? HIGH : LOW);
}
```

- [ ] **Step 2: `loop()` 调用渲染**

```cpp
void loop() {
  server.handleClient();
  renderLeds();
}
```

- [ ] **Step 3: 编译、烧录**

- [ ] **Step 4: 验证灯效(肉眼)**

```bash
unset http_proxy https_proxy all_proxy HTTP_PROXY HTTPS_PROXY ALL_PROXY
B=http://devlight.local
curl -s "$B/set?agent=claude&state=working"   # 期望:黄灯闪
sleep 3
curl -s "$B/set?agent=claude&state=confirm"    # 期望:红灯闪
sleep 3
curl -s "$B/set?agent=claude&state=idle"       # 期望:绿灯常亮
```
Expected(肉眼确认):黄闪 → 红闪 → 绿常亮。

- [ ] **Step 5: 提交**

---

## Task 5: 离线指示 + WiFi 自动重连

**Files:**
- Modify: `esp32-devlight/esp32-devlight.ino`(加 mode 状态、重连、离线灯效)

- [ ] **Step 1: 加在线/离线判断与重连(在 LED 渲染里分支)**

`renderLeds()` 改为先判断 WiFi:
```cpp
void renderLeds() {
  if (WiFi.status() != WL_CONNECTED) {
    // 离线:三灯一起慢闪 ~0.5Hz(1s 亮 / 1s 灭)
    bool slowOn = (millis() / 1000) % 2 == 0;
    digitalWrite(PIN_G, slowOn?HIGH:LOW);
    digitalWrite(PIN_Y, slowOn?HIGH:LOW);
    digitalWrite(PIN_R, slowOn?HIGH:LOW);
    return;
  }
  Agg a = aggregate();
  bool blinkOn = (millis() / 250) % 2 == 0;
  digitalWrite(PIN_G, a==AGG_GREEN ? HIGH : LOW);
  digitalWrite(PIN_Y, (a==AGG_YELLOW && blinkOn) ? HIGH : LOW);
  digitalWrite(PIN_R, (a==AGG_RED   && blinkOn) ? HIGH : LOW);
}
```

- [ ] **Step 2: 在 `loop()` 加非阻塞重连(每 5s 检查一次)**

```cpp
unsigned long lastWifiCheck = 0;
// loop():
void loop() {
  server.handleClient();
  if (millis() - lastWifiCheck > 5000) {
    lastWifiCheck = millis();
    if (WiFi.status() != WL_CONNECTED) { Serial.println("WiFi lost, reconnecting..."); WiFi.reconnect(); }
  }
  renderLeds();
}
```
(把 `lastWifiCheck` 声明放到全局区。)

- [ ] **Step 3: 编译、烧录**

- [ ] **Step 4: 验证离线指示**

断网测试(任选):关掉热点 / 让 ESP32 离开 WiFi 范围 / 路由器断电。
Expected:断网后约 5–10s 内三灯开始一起慢闪;恢复网络后自动重连,灯回到聚合状态(绿/黄/红)。

- [ ] **Step 5: 提交**

---

## Task 6: Captive Portal 配网(无凭据/连不上时回退)

**Files:**
- Modify: `esp32-devlight/esp32-devlight.ino`(加 AP 模式、DNS 劫持、配网页、存 NVS、重启)

- [ ] **Step 1: 加全局 DNSServer 与 portal 标志**

全局区加:
```cpp
DNSServer dnsServer;
bool portalMode = false;
```

- [ ] **Step 2: 加配网页与提交处理**

```cpp
void handlePortalRoot() {
  server.send(200, "text/html",
    "<h3>DevLight 配网</h3>"
    "<form action='/save' method='POST'>"
    "WiFi 名称(SSID):<br><input name='ssid'><br>"
    "WiFi 密码:<br><input name='pass' type='password'><br>"
    "设备名(如 desk,可留空):<br><input name='name'><br><br>"
    "<button type='submit'>保存并重启</button></form>");
}
void handlePortalSave() {
  String ssid = server.arg("ssid");
  String pass = server.arg("pass");
  String name = server.arg("name");
  if (ssid.length()==0) { server.send(400,"text/plain","ssid required\n"); return; }
  prefs.putString("ssid", ssid);
  prefs.putString("pass", pass);
  prefs.putString("name", name);   // 空也存,deviceHostname() 会回退芯片ID
  server.send(200, "text/html", "<p>已保存,正在重启...</p>");
  delay(1500);
  ESP.restart();
}
void startPortal() {
  portalMode = true;
  Serial.println("Starting captive portal AP: DevLight-Setup");
  WiFi.mode(WIFI_AP);
  WiFi.softAP("DevLight-Setup");
  IPAddress apIP = WiFi.softAPIP();
  Serial.print("AP IP: "); Serial.println(apIP);
  dnsServer.start(53, "*", apIP);              // 把所有域名劫持到本机
  server.on("/", handlePortalRoot);
  server.on("/save", HTTP_POST, handlePortalSave);
  server.onNotFound(handlePortalRoot);          // captive portal 探测页也导到配网
  server.begin();
}
```

- [ ] **Step 3: `setup()` 改为"连不上就进 portal"**

`setup()` 里 `connectWifi();` 之后改为:
```cpp
  if (WiFi.status() == WL_CONNECTED) {
    server.on("/", handleRoot);
    server.on("/set", handleSet);
    server.begin();
  } else {
    startPortal();
  }
```
(删掉原先无条件的 `server.on("/",...)/server.begin()`。)

- [ ] **Step 4: `loop()` 在 portal 模式下处理 DNS**

```cpp
void loop() {
  if (portalMode) { dnsServer.processNextRequest(); server.handleClient(); renderLeds(); return; }
  server.handleClient();
  if (millis() - lastWifiCheck > 5000) { lastWifiCheck = millis(); if (WiFi.status()!=WL_CONNECTED){ Serial.println("WiFi lost, reconnecting..."); WiFi.reconnect(); } }
  renderLeds();
}
```
(portal 模式下 `WiFi.status()!=WL_CONNECTED`,renderLeds 会走三灯慢闪——正好表示"未配网"。)

- [ ] **Step 5: 验证 portal 回退**

把 `secrets.h` 的 SSID/PASS 清空并清 NVS(首次烧录新固件时 NVS 里无 ssid,getString 回退到空 secrets → 连接失败):
```bash
# 清空 secrets.h 后编译烧录
unset http_proxy https_proxy all_proxy HTTP_PROXY HTTPS_PROXY ALL_PROXY
/opt/homebrew/bin/arduino-cli compile --fqbn esp32:esp32:esp32 /Users/bytedance/esp32-devlight
/opt/homebrew/bin/arduino-cli upload -p /dev/cu.wchusbserial10 --fqbn esp32:esp32:esp32 /Users/bytedance/esp32-devlight
```
Expected:串口打印 `Starting captive portal AP: DevLight-Setup` + AP IP;三灯慢闪。手机 WiFi 列表出现 `DevLight-Setup`,连上后弹出/访问配网页,填真实 WiFi → 保存 → ESP32 重启并连入(串口再次打印 IP)。

- [ ] **Step 6: 提交**

---

## Task 7: Mac 侧 `devlight-set` 复用脚本 + Claude Code hooks

**Files:**
- Create: `esp32-devlight/devlight-set`
- Create: `~/.config/devlight/url`
- Modify: `~/.claude/settings.json`

> 用 superpowers:update-config 或下面的 python 合并脚本,**务必保留**现有 `SessionStart`/`SessionEnd`(mempalace 同步)。

- [ ] **Step 1: 写 `devlight-set` 复用脚本(单一配置点)**

`esp32-devlight/devlight-set`:
```bash
#!/usr/bin/env bash
# 用法: devlight-set <claude|codex> <idle|working|confirm>
# 目标地址来自 ~/.config/devlight/url(默认 http://devlight.local)
agent="$1"; state="$2"
[ -z "$agent" ] || [ -z "$state" ] && exit 0
cfg="$HOME/.config/devlight/url"
base="http://devlight.local"
[ -f "$cfg" ] && base="$(tr -d '[:space:]' < "$cfg")"
# 绕过代理直连局域网设备
curl -s --noproxy '*' --max-time 1 "$base/set?agent=$agent&state=$state" >/dev/null 2>&1 || true
```
```bash
chmod +x /Users/bytedance/esp32-devlight/devlight-set
```

- [ ] **Step 2: 设本机目标地址(用这盏灯实际的主机名)**

```bash
mkdir -p ~/.config/devlight
echo "http://devlight-XXXX.local" > ~/.config/devlight/url   # 换成串口/配网后的真实主机名
/Users/bytedance/esp32-devlight/devlight-set claude idle      # 自测:灯应变绿
curl -s --noproxy '*' --max-time 2 "$(cat ~/.config/devlight/url)/" ; echo
```
Expected:`GET /` 显示 `claude=idle`,灯绿。

- [ ] **Step 3: 先备份并查看现有 hooks**

```bash
cp ~/.claude/settings.json ~/.claude/settings.json.bak.$(date +%s)
python3 -c "import json;d=json.load(open('/Users/bytedance/.claude/settings.json'));print(json.dumps(d.get('hooks',{}),indent=2,ensure_ascii=False))"
```
Expected:看到现有 SessionStart/SessionEnd 命令(记录下来,SessionEnd 要追加不是替换)。

- [ ] **Step 4: 用 python 安全合并 hooks(调 devlight-set)**

```bash
python3 - <<'PY'
import json
p='/Users/bytedance/.claude/settings.json'
SET='/Users/bytedance/esp32-devlight/devlight-set'
d=json.load(open(p))
hooks=d.setdefault('hooks',{})
def cmd(state):
    return {"type":"command","command": f"{SET} claude {state}"}
def ensure(event, state):
    arr=hooks.setdefault(event,[])
    for grp in arr:                       # 避免重复
        for h in grp.get('hooks',[]):
            if 'devlight-set' in h.get('command',''): return
    arr.append({"hooks":[cmd(state)]})
ensure('UserPromptSubmit','working')
ensure('Notification','confirm')
ensure('Stop','idle')
ensure('SessionEnd','idle')   # 追加,不动现有 SessionEnd 命令
json.dump(d,open(p,'w'),indent=2,ensure_ascii=False)
print('done; events:',list(hooks.keys()))
PY
```
Expected:打印包含 UserPromptSubmit/Notification/Stop/SessionEnd/SessionStart。

- [ ] **Step 5: 校验 JSON 合法 + 现有命令仍在**

```bash
python3 -c "import json;d=json.load(open('/Users/bytedance/.claude/settings.json'));print('SessionEnd 组数:',len(d['hooks']['SessionEnd']));print(json.dumps(d['hooks']['SessionEnd'],ensure_ascii=False,indent=2))"
```
Expected:JSON 合法;SessionEnd 同时含原 mempalace 命令与新 devlight 命令。

- [ ] **Step 6: 验证 hook 真正触发(需新开一个 Claude Code 会话使 settings 生效)**

新会话里发一条消息观察:发消息瞬间灯变黄闪(working),回答结束变绿(idle);触发一次权限提示时变红闪(confirm)。也可串口看 `set claude=...` 日志。
Expected:灯随 Claude 状态切换。若 `Notification` 误报(闲置提醒也变红),记录到 spec §6 第 2 点,下一步按 message 内容过滤。

- [ ] **Step 7: 提交 `devlight-set` 入库**(`~/.claude/settings.json` 在 home,不入本仓库;`~/.config/devlight/url` 也不入库)
```bash
cd /Users/bytedance/esp32-devlight && git add devlight-set && git commit -q -m "feat: 加 devlight-set 复用脚本 + Claude hooks 接入"
```

---

## Task 8: Codex notify 集成(先实测事件,再定映射)

**Files:**
- Create: `esp32-devlight/devlight-codex-notify.sh`
- Modify: `~/.codex/config.toml`

- [ ] **Step 1: 写 notify 脚本(带事件落盘,便于实测)**

`esp32-devlight/devlight-codex-notify.sh`:
```bash
#!/usr/bin/env bash
# Codex 用单个 JSON 字符串参数调用本脚本。
payload="$1"
# 实测期:把每次事件 JSON 追加到日志,方便观察 Codex 实际 emit 了什么
echo "$(date -u +%FT%TZ) $payload" >> /tmp/devlight-codex-events.log

type=$(printf '%s' "$payload" | /usr/bin/python3 -c "import sys,json; print(json.load(sys.stdin).get('type',''))" 2>/dev/null)
case "$type" in
  agent-turn-complete) state=idle ;;     # 回合结束 -> 绿
  *)                   state=idle ;;     # 未知事件先保守归 idle(实测后再细化)
esac
# 复用 Mac 侧脚本(单一配置点)
"$(dirname "$0")/devlight-set" codex "$state"
```
```bash
chmod +x /Users/bytedance/esp32-devlight/devlight-codex-notify.sh
```

- [ ] **Step 2: 用样例 JSON 离线测试脚本解析(不依赖 Codex)**

```bash
/Users/bytedance/esp32-devlight/devlight-codex-notify.sh '{"type":"agent-turn-complete","last-assistant-message":"done"}'
tail -1 /tmp/devlight-codex-events.log
curl -s --noproxy '*' --max-time 2 "$(cat ~/.config/devlight/url)/" ; echo
```
Expected:日志追加一行;`GET /` 显示 `codex=idle`(脚本能跑通、经 devlight-set 命中正确地址)。

- [ ] **Step 3: 配置 Codex notify**

```bash
python3 - <<'PY'
import re,os
p=os.path.expanduser('~/.codex/config.toml')
s=open(p).read() if os.path.exists(p) else ''
line='notify = ["/Users/bytedance/esp32-devlight/devlight-codex-notify.sh"]'
if 'devlight-codex-notify.sh' in s:
    print('already set')
else:
    if re.search(r'(?m)^notify\s*=', s):
        s=re.sub(r'(?m)^notify\s*=.*$', line, s)   # 覆盖已有 notify(注意:会顶掉旧 notify)
    else:
        s=(s.rstrip()+'\n'+line+'\n') if s else line+'\n'
    open(p,'w').write(s)
    print('written')
PY
cat ~/.codex/config.toml | grep notify
```
Expected:config.toml 含该 notify 行。
> ⚠️ 若已有别的 `notify`,上面会覆盖——执行时先 `cat` 看一眼,有冲突手动合并。

- [ ] **Step 4: 实测 Codex 真实事件并据此修订映射**

跑一次真实 Codex 会话(让它干点活、结束、必要时触发审批),然后:
```bash
cat /tmp/devlight-codex-events.log
```
观察 Codex 实际 emit 了哪些 `type`。**据此修订 Step 1 的 case 分支**:例如若有 turn-start/审批相关事件,映射到 working/confirm;若确实只有 `agent-turn-complete`,则 Codex 只能表达"回合结束=绿",working 只能用"近似"(如:notify 前置一个 wrapper,或接受 Codex 仅 idle/working 两态)。把结论写回 spec §6 第 1 点。

- [ ] **Step 5: 验证端到端**

跑 Codex 一个回合,观察灯:回合中(若可检测)黄闪,结束绿。
Expected:Codex 状态能反映到灯(粒度以实测为准)。

- [ ] **Step 6: 提交**

---

## Task 9: 端到端联调 + 收尾文档

**Files:**
- Create: `esp32-devlight/README.md`

- [ ] **Step 1: 双 agent 联调**

同时:Claude Code 会话发消息(claude=working)+ Codex 触发审批(codex=confirm)。
Expected:红优先(红闪);Codex 结束且 Claude 仍在跑 → 黄闪;都结束 → 绿。

- [ ] **Step 2: 断网联调**

联调中途断 WiFi → 三灯慢闪;恢复 → 回到聚合状态。

- [ ] **Step 3: 写 README(面向复用 + GitHub)**

`esp32-devlight/README.md` 必含:
- 项目简介 + 灯色映射表(绿/黄闪/红闪/离线慢闪)。
- **接一盏新灯的 4 步**:① clone + `arduino-cli compile/upload`(贴 FQBN 与镜像 unset 命令);② 首次开机连 AP `DevLight-Setup` 配网 + 起设备名;③ Mac 侧 `cp devlight-set` 入 PATH(或用绝对路径)+ `echo http://devlight-<名>.local > ~/.config/devlight/url`;④ 装 hooks(贴 Task 7 的 python)+ Codex notify(贴 Task 8)。
- 接线表(25/26/27/-)、`secrets.h` 可选预置说明、`GET /` 调试。
- 已知限制:Codex notify 粒度、Claude `Notification` 语义、mDNS 可达性(spec §6)。
- 安全:`secrets.h` 与 `~/.config/devlight/url` 不入库;WiFi 密码走配网或本地 secrets,绝不进 git。

- [ ] **Step 4: 提交 + 准备推 GitHub**
```bash
cd /Users/bytedance/esp32-devlight
git add README.md esp32-devlight.ino devlight-codex-notify.sh docs
git commit -q -m "docs: README + 固件/脚本收尾,可复用"
# 推送(到时候):git remote add origin <repo-url> && git push -u origin main
```

---

## Self-Review

**Spec coverage(逐条对 spec):**
- §1 可复用/一机一灯/唯一命名 → Task 2 `deviceHostname()` + Task 6 配网设备名 + Task 7 `devlight-set` 单一配置点 + Task 9 README ✓
- §2 状态映射(绿常亮/黄闪/红闪/离线慢闪)→ Task 4 + Task 5 ✓
- §2 聚合优先级红>黄>绿 → Task 3 `aggregate()` ✓
- §3 数据流 + mDNS(`devlight-<名>.local`)→ Task 2 ✓
- §4.1 HTTP `/set` `/`、NVS、设备命名、非阻塞闪烁、重连、captive portal → Task 2/3/4/5/6 ✓
- §4.2 `devlight-set` 复用脚本 + 配置文件 → Task 7 Step1-2 ✓
- §4.3 Claude hooks(追加保留现有,调 devlight-set)→ Task 7 Step3-7 ✓
- §4.4 Codex notify(调 devlight-set)→ Task 8 ✓
- §5 错误处理(curl --max-time/--noproxy/||true、重连、NVS) → Task 7/8 + Task 5/6 ✓
- §6 不确定点 → Task 8 Step4(Codex)、Task 7 Step6(Notification)、Task 2 Step4(mDNS)均有实测步骤 ✓
- §7 产物清单(含 devlight-set/README/git)→ 全部任务覆盖 ✓

**Placeholder scan:** 无 TBD/TODO;唯一"实测后修订"的是 Codex 映射(Task 8 Step4),这是 spec 明确的开放点,已给出实测方法与兜底策略,非占位符。

**Type consistency:** `SubState{ST_IDLE/ST_WORKING/ST_CONFIRM}`、`Agg{AGG_GREEN/YELLOW/RED}`、`aggregate()`/`parseState()`/`subName()`/`aggName()`/`renderLeds()`/`connectWifi()`/`startPortal()` 全程一致;Prefs namespace `"wifi"` 键 `ssid`/`pass` 一致;端点 `/`、`/set`、`/save` 一致;agent 取值 `claude`/`codex`、state 取值 `idle`/`working`/`confirm` 全程一致。

---

## 执行注意
- 编译/烧录/curl 前都要 `unset ...proxy`(直连)。
- 烧录会占用串口;`arduino-cli monitor` 与 upload 不能同时占用,先 Ctrl-C 退出 monitor 再 upload。
- Task 7 改的是 `~/.claude/settings.json`(全局,影响所有会话),改前已备份。
